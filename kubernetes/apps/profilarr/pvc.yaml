apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: profilarr-config
  namespace: profilarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-single
  resources:
    requests:
      storage: 5Gi
