apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: thelounge-config
  namespace: thelounge
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
