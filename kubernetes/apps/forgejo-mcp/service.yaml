apiVersion: v1
kind: Service
metadata:
  name: forgejo-mcp
  namespace: forgejo-mcp
  labels:
    app: forgejo-mcp
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: forgejo-mcp
