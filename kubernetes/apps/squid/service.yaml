apiVersion: v1
kind: Service
metadata:
  name: squid
  namespace: squid
  labels:
    app: squid
spec:
  type: ClusterIP
  ports:
    - port: 3128
      targetPort: 3128
      protocol: TCP
      name: http
  selector:
    app: squid
