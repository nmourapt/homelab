apiVersion: v1
kind: Service
metadata:
  name: booklore
  namespace: booklore
  labels:
    app: booklore
spec:
  type: ClusterIP
  ports:
    - port: 6060
      targetPort: 6060
      protocol: TCP
      name: http
  selector:
    app: booklore
